import styled from 'styled-components';

const Button = styled.a`
  color: #bdbdbd;
  cursor: pointer;
  position: absolute;
  top: 0;
  right: 5px;
  font-size: 14px;

  
  &:hover {
    color: #f96868;
  }
`;

export default Button;
